# ===========================================
# VDID Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ===========================================
# Required
# ===========================================

# Environment (development | production | test)
NODE_ENV=development

# Server port
PORT=5000

# PostgreSQL Database URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/vdid

# JWT Secret (minimum 32 characters, MUST change in production!)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars

# ===========================================
# Optional
# ===========================================

# JWT Issuer
JWT_ISSUER=vdid.io

# Email Service (Resend)
# Get your API key from https://resend.com
RESEND_API_KEY=
EMAIL_FROM=noreply@yourdomain.com

# WebAuthn Configuration
WEBAUTHN_RP_ID=localhost
WEBAUTHN_ORIGIN=http://localhost:5000

# Frontend URL (for CORS and email links)
CLIENT_URL=http://localhost:5000

# ===========================================
# Production Checklist
# ===========================================
# [ ] Set NODE_ENV=production
# [ ] Use strong, unique JWT_SECRET (32+ chars)
# [ ] Configure DATABASE_URL for production DB
# [ ] Set up RESEND_API_KEY for email
# [ ] Update WEBAUTHN_RP_ID to your domain
# [ ] Update CLIENT_URL to production URL
